# MQTT Settings
MQTT_HOST=servicez.cloud
MQTT_PORT=1883
MQTT_USERNAME=rez
MQTT_PASSWORD=ukn8qnc2QYP@acz4pjd

# Printer Settings
PRINTER_MEDIA=2x1              # Label size/type (not a CUPS name)
PRINTER_DEVICE=/dev/usb/lp0    # USB device path

# Monitoring
STATUS_INTERVAL=10000          # Status publish interval (ms)
WIFI_INTERFACE=wlan0           # Interface for signal monitoring

# LED
LED_BRIGHTNESS=0.5             # Default LED brightness (0-1)